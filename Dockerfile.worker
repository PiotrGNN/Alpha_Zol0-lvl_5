# Dockerfile for ZoL0 Worker (bot only, for Render Background Worker)
FROM python:3.10-slim
WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir -r requirements.txt
CMD ["python", "start_worker.py"]
